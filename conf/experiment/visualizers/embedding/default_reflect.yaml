# @package _group_

type: embedding

run_on_test: False
save_data: False

no_over_fields: []

fields:
  distances:
    use_abs: False
    normalize: True

  normal:
    use_abs: True
    normalize: False
    bounds: [0.0, 1.0]

  render_normal:
    use_abs: True
    normalize: False
    bounds: [0.0, 1.0]

  point_offset:
    use_abs: True
    bounds: [0.0, 0.25]

