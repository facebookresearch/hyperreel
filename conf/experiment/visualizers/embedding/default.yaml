# @package _group_

type: embedding

run_on_test: False
save_data: False

no_over_fields: []

fields:
  distances:
    use_abs: False
    normalize: True

  point_offset:
    use_abs: True
    bounds: [0.0, 0.25]

  points:
    use_abs: False
    bounds: [-2.0, 2.0]
