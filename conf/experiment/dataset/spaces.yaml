# @package _group_

name: spaces
collection: scene_000
data_subdir: spaces
root_dir: "${experiment.params.data_dir}/${experiment.dataset.data_subdir}/${experiment.dataset.collection}/"

img_wh: [800, 480]
spherical_poses: False
use_ndc: True

val_num: 8
val_skip: 8
val_all: False

render_params:
  interpolate: False
  supersample: 4
  crop: 1.0
